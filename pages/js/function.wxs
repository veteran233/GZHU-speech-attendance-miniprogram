searchFunction = function (str, patten) {
  if (patten == '') return true
  else if (str.indexOf(patten) != -1) return true
  else return false
}

module.exports = {
  searchFunction: searchFunction
}